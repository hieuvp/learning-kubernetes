apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "study.fullname" . }}-test-connection"
  labels:
{{ include "study.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "study.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
