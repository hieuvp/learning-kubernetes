apiVersion: v1
kind: Service
metadata:
  name: {{ include "study.fullname" . }}
  labels:
    {{- include "study.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "study.selectorLabels" . | nindent 4 }}
